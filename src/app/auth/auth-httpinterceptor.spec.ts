import { AuthHTTPInterceptor } from './auth-httpinterceptor';

describe('AuthHTTPInterceptor', () => {
  it('should create an instance', () => {
    expect(new AuthHTTPInterceptor()).toBeTruthy();
  });
});
